# THE CONTRACTORS DATASET's local data - never in git. The raw data's
# permanent home is the raw-archive GitHub Release (see NOTES.md,
# PIPELINE v2). In git from this folder: scripts, .bats, NOTES.md, tests,
# and archive/manifest.json.
inputs/
out/
__pycache__/
